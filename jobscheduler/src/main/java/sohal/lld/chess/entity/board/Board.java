package sohal.lld.chess.entity.board;import sohal.lld.chess.entity.Color;import sohal.lld.chess.entity.playable.*;import sohal.lld.chess.exception.InvalidMoveException;public class Board {    final Cell[][] board;    public Board() {        this.board = new Cell[8][8];        for (int row = 0; row < 8; row++) {            for (int col = 0; col < 8; col++) {                board[row][col] = new Cell(row, col);            }        }        initBoard();    }    private void initBoard() {        //Add pawns to both sides        for (int j = 0; j < 8; j++) {            board[1][j].setPiece(new Pawn(Color.WHITE));            board[6][j].setPiece(new Pawn(Color.BLACK));        }            // Setup main pieces            board[0][0].setPiece(new Rook(Color.WHITE));            board[0][1].setPiece(new Knight(Color.WHITE));            board[0][2].setPiece(new Bishop(Color.WHITE));            board[0][3].setPiece(new Queen(Color.WHITE));            board[0][4].setPiece(new King(Color.WHITE));            board[0][5].setPiece(new Bishop(Color.WHITE));            board[0][6].setPiece(new Knight(Color.WHITE));            board[0][7].setPiece(new Rook(Color.WHITE));            board[7][0].setPiece(new Rook(Color.BLACK));            board[7][1].setPiece(new Knight(Color.BLACK));            board[7][2].setPiece(new Bishop(Color.BLACK));            board[7][3].setPiece(new Queen(Color.BLACK));            board[7][4].setPiece(new King(Color.BLACK));            board[7][5].setPiece(new Bishop(Color.BLACK));            board[7][6].setPiece(new Knight(Color.BLACK));            board[7][7].setPiece(new Rook(Color.BLACK));    }    public Cell getCell(int row, int col) {        return board[row][col];    }    public Piece getPiece(int row, int col) {        return board[row][col].getPiece();    }    public void setPiece(int row, int col, Piece piece) {        board[row][col].setPiece(piece);    }    public synchronized boolean movePiece(Move move) {        Cell from = move.getStart();        Cell to = move.getEnd();        Piece piece = from.getPiece();        if(piece == null || !piece.canMove(this, from, to)) {            return false;        }        Color playerColor = piece.getColor();        Piece capturedPiece = to.getPiece();        setPiece(to.getRow(), to.getCol(), piece);        setPiece(from.getRow(), from.getCol(), null);        boolean inCheck = isUnderAttack(findKing(playerColor), playerColor);        if(inCheck) {            setPiece(from.getRow(), from.getCol(), piece);            setPiece(to.getRow(), to.getCol(), capturedPiece);        }        from.setPiece(null);        if(capturedPiece != null) {            System.out.println("Captured " + capturedPiece.getColor() + " " +capturedPiece.getClass().getSimpleName());        }        return true;    }    public boolean isUnderAttack(Cell target, Color kingColor) {        for (int row = 0; row < 8; row++) {            for (int col = 0; col < 8; col++) {                Piece piece = getPiece(row, col);                if (piece != null && piece.getColor() != kingColor) {                    Cell start = new Cell(row, col);                    if(piece.canMove(this, start, target)) {                        return true;                    }                }            }        }        return false;    }    public boolean isCheckMate(Color kingColor) {        // king must be currently in check        if(!isCheck(kingColor)) {            return false;        }        // Try all possible moves for all pieces of kings color        for (int r =0; r < 8; r++) {            for (int c =0; c < 8; c++) {                Piece piece = getPiece(r, c);                if (piece != null && piece.getColor() == kingColor) {                    Cell start = new Cell(r, c);                    for (int r2 =0; r2 < 8; r2++) {                        for (int c2 =0; c2 < 8; c2++) {                            Cell end = new Cell(r2, c2);                            if(piece.canMove(this, start, end)) {                                //simulate the move                                Piece capturedPiece = getPiece(r2, c2);                                this.setPiece(r2, c2, piece);                                this.setPiece(r, c, null);                                boolean stillInCheck = isCheck(kingColor);                                //undo move                                this.setPiece(r, c, piece);                                this.setPiece(r2, c2, capturedPiece);                                if(!stillInCheck) {                                    return false;                                }                            }                        }                    }                }            }        }        //if no move removes check : then checkmate        return true;    }    private boolean isCheck(Color kingColor) {        Cell kingCell = findKing(kingColor);        return isUnderAttack(kingCell, kingColor);    }    private Cell findKing(Color kingColor) {        for (int row = 0; row < 8; row++) {            for (int col = 0; col < 8; col++) {                Piece piece = getPiece(row, col);                if (piece instanceof King && piece.getColor() == kingColor) {                    return new Cell(row, col);                }            }        }        throw new InvalidMoveException(kingColor +" King not found");    }    public boolean isStaleMate(Color kingColor) {        //if king is not in check        if(isCheck(kingColor)) {            return false;        }        for (int r = 0; r < 8; r++) {            for (int c = 0; c < 8; c++) {                Piece piece = getPiece(r, c);                if (piece != null && piece.getColor() == kingColor) {                    Cell start = new Cell(r, c);                    for (int r2 =0; r2 < 8; r2++) {                        for (int c2 =0; c2 < 8; c2++) {                            Cell end = new Cell(r2, c2);                            if(piece.canMove(this, start, end)) {                                // Simulate move                                Piece capturedPiece = getPiece(r2, c2);                                this.setPiece(r2, c2, piece);                                this.setPiece(r, c, null);                                boolean resultsInCheck = isCheck(kingColor);                                //undo move                                this.setPiece(r, c, piece);                                this.setPiece(r2, c2, capturedPiece);                                if(!resultsInCheck) {                                    return false;                                }                            }                        }                    }                }            }        }        return true;    }    public void printBoard() {        System.out.println("   0 1 2 3 4 5 6 7");        for (int r = 7; r >= 0; r--) {            System.out.print(r + "  ");            for (int c = 0; c < 8; c++) {                Piece piece = getPiece(r, c);                if (piece == null) System.out.print(". ");                else {                    char symbol = getPieceSymbol(piece);                    System.out.print(symbol + " ");                }            }            System.out.println();        }    }    private char getPieceSymbol(Piece piece) {        char sym;        if (piece instanceof King) sym = 'K';        else if (piece instanceof Queen) sym = 'Q';        else if (piece instanceof Rook) sym = 'R';        else if (piece instanceof Bishop) sym = 'B';        else if (piece instanceof Knight) sym = 'N';        else sym = 'P'; // pawn        return piece.getColor() == Color.WHITE ? Character.toUpperCase(sym) : Character.toLowerCase(sym);    }}