package sohal.lld.chess.entity.playable;import sohal.lld.chess.entity.Color;import sohal.lld.chess.entity.board.Board;import sohal.lld.chess.entity.board.Cell;public class King extends Piece{    public King(Color color) {        super(color);    }    @Override    public boolean canMove(Board board, Cell start, Cell end) {        // destination piece        Piece destPiece = board.getPiece(end.getRow(), end.getCol());        if (destPiece != null && destPiece.getColor() == this.getColor()) return false;        int startRow = start.getRow();        int startCol = start.getCol();        int endRow = end.getRow();        int endCol = end.getCol();        int rowDiff = Math.abs(startRow - endRow);        int colDiff = Math.abs(startCol - endCol);        if (rowDiff > 1 || colDiff > 1) return false;        // simulate the move to check the 'check' possibility        Piece originPiece = destPiece;        board.setPiece(end.getRow(), end.getCol(), this); // move to end pos        board.setPiece(start.getRow(), start.getCol(), null); // remove from start pos        boolean isInCheck = board.isUnderAttack(end, this.getColor());        //Revert move        board.setPiece(start.getRow(), start.getCol(), this);        board.setPiece(end.getRow(), end.getCol(), originPiece);        return !isInCheck;    }}