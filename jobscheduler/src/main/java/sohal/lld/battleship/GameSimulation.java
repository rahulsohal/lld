package sohal.lld.battleship;import sohal.lld.battleship.firing.RandomFireStrategy;import sohal.lld.battleship.logging.GameLogger;import sohal.lld.battleship.ship.Ship;import sohal.lld.battleship.ship.ShipFactory;public class GameSimulation {    public static void main(String[] args) {        GameSimulation gameSimulation = new GameSimulation();        Game game = new Game(10); // Example battlefield 10x10        game.initGame();// Create ships using factory        Ship a1 = ShipFactory.createSquareShip("S1", 3, 2, 2);        Ship b1 = ShipFactory.createSquareShip("S1", 3, 7, 2);        Ship a2 = ShipFactory.createSquareShip("S2", 4, 3, 6);        Ship b2 = ShipFactory.createSquareShip("S2", 4, 6, 6);        Ship a3 = ShipFactory.createSquareShip("S3", 2, 1, 8);        Ship b3 = ShipFactory.createSquareShip("S3", 2, 8, 8);// Add ships        game.addShipToBoth("S1", a1, b1);        game.addShipToBoth("S2", a2, b2);        game.addShipToBoth("S3", a3, b3);// Add logger observer        GameLogger logger = new GameLogger();        game.addObserver(logger);// Show battlefield before start        System.out.println("=== Initial Battlefield ===");        System.out.println(game.viewBattleField());// Use random strategies       RandomFireStrategy stratA = new RandomFireStrategy(new java.util.Random());       RandomFireStrategy stratB = new RandomFireStrategy(new java.util.Random());// Start game        game.startGame(stratA, stratB);// Show logs        System.out.println("=== Game Log ===");        logger.printLog();// Show final battlefield        System.out.println("=== Final Battlefield ===");        System.out.println(game.viewBattleField());    }}